[sensor]
udp_host = "127.0.0.1"
udp_port = 50050

rate_hz = 10

probe_radius = 0.03
probe_height = 0.05
probe_color = [1.0, 1.0, 0.0]

default_offset = [0.0, 0.0, 0.25]

# Scenario scripts can override this at runtime.
apply_source_self_attenuation = false
